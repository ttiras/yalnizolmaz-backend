CREATE TABLE "public"."contribution_likes" ("created_at" timestamptz NOT NULL DEFAULT now(), "contribution_id" uuid NOT NULL, "user_id" uuid NOT NULL, PRIMARY KEY ("contribution_id","user_id") , FOREIGN KEY ("contribution_id") REFERENCES "public"."contributions"("id") ON UPDATE no action ON DELETE cascade, FOREIGN KEY ("user_id") REFERENCES "auth"."users"("id") ON UPDATE no action ON DELETE set null);
